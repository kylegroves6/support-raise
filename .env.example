# Copy to .env and fill in your Supabase project values.
# These are safe to expose client-side (anon key is not a secret).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>
